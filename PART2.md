# Part 2 – Product Understanding: AbleSpace Caseload / Take Data

> **Note:** I don't have access to your AbleSpace account, so I can't
> actually explore the live Caseload → Take Data screen myself — this file
> is a **template** for you to fill in after exploring it, based on the
> screenshot in the assignment PDF and your own click-through. Replace
> every `[ ... ]` below with your own observations and screenshots.

## 1. Where this screen lives

Caseload tab (left sidebar, under "CAPTURE") → student list → **Take Data**
button on a student's row.

## 2. Workflow, in my own words

1. [Describe what happens when you land on the Caseload tab — what's the
   default view, what columns are shown (Full Name, Last Name, IEP Due,
   Eval Due, Collaborators, Service Time, School, Actions per the
   screenshot).]
2. [Click "Take Data" for a student — describe what opens: a new screen? a
   modal? What data-entry fields appear?]
3. [Walk through actually logging a data point — what do you select/type,
   what happens when you save?]
4. [What happens after saving — does it return to the Caseload list, show
   a confirmation, update a count somewhere?]

*(Insert screenshots at each step, numbered to match.)*

## 3. UX / functionality observations

Use this section for anything you notice while clicking through — for
example:
- Is the search bar's shortcut (`⌘ + k` shown in the screenshot)
  discoverable to a first-time user?
- Are the group filters ("Students (15)", "Groups (12)", "Unassigned
  (39)") easy to tell apart from tabs vs. counts?
- Is the "Take Data" action clear enough for a new user, or would a label
  like "Log Session" be more intuitive?
- Any friction points when a student has no due dates (`-`) — is that
  handled gracefully in the UI?

## 4. Suggested improvements

| Area | Current behavior | Suggested change | Why |
|---|---|---|---|
| [ ] | [ ] | [ ] | [ ] |
| [ ] | [ ] | [ ] | [ ] |

---
*Submit this either as this filled-in document with screenshots embedded,
or as a video walkthrough per the assignment instructions.*
